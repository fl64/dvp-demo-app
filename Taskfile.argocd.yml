# https://taskfile.dev
# ArgoCD tasks for managing ApplicationSets

version: "3"

tasks:
  apply:
    desc: Apply ArgoCD ApplicationSet manifests
    cmds:
      - kubectl apply -f argocd/projects.yaml
      - kubectl apply -f argocd/apps.yaml

  delete:
    desc: Delete ArgoCD ApplicationSet manifests
    cmds:
      - kubectl delete -f argocd/apps.yaml || true
      - kubectl delete -f argocd/projects.yaml || true
